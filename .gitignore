# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Vite cache
.vite/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~

# Environment & secrets — NEVER commit tokens.
.env
.env.*
!.env.example

# Local AI agent settings (no relevance to project consumers).
.claude/

# TypeScript build info
*.tsbuildinfo

# Temp frames captured by scripts/screenshot.mjs before WebP encoding.
.preview-frames/

# NOTE: public/data/contributions.json is intentionally NOT ignored.
# It is the generated data the deployed site reads, committed by the
# update-data workflow.
