import type { Layer, FrameContext } from "./types";

/** §2.2 project hover highlight — implemented in Task 10. */
export class ProjectHighlightLayer implements Layer {
  draw(_f: FrameContext): void {}
}
